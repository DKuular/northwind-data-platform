name: northwind-spark

services:
  spark-master:
    image: apache/spark:3.5.0
    container_name: northwind-spark-master
    command: /opt/spark/bin/spark-class org.apache.spark.deploy.master.Master
    ports:
      - "8085:8080"
      - "7077:7077"
    networks:
      - spark-network
      - data-platform-network
    restart: unless-stopped

  spark-worker:
    image: apache/spark:3.5.0
    container_name: northwind-spark-worker
    command: /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker spark://spark-master:7077
    ports:
      - "8082:8081"
    networks:
      - spark-network
      - data-platform-network
    depends_on:
      - spark-master
    restart: unless-stopped

  jupyter:
    build:
      context: .
      dockerfile: Dockerfile.jupyter
    image: northwind-jupyter:3.5.0
    container_name: northwind-jupyter
    environment:
      JUPYTER_ENABLE_LAB: "yes"
      JUPYTER_TOKEN: "spark_user"
    ports:
      - "8888:8888"
    networks:
      - spark-network
      - data-platform-network
    volumes:
      - ./notebooks:/home/jovyan/work
      - ./apps:/home/jovyan/work/apps
      - ivy-cache-jupyter:/home/jovyan/.ivy2
    restart: unless-stopped

  bronze-customers:
    image: northwind-jupyter:3.5.0
    container_name: northwind-bronze-customers
    command: >
      /usr/local/spark/bin/spark-submit
      --master spark://spark-master:7077
      --jars
      /opt/spark-extra-jars/spark-sql-kafka-0-10_2.12-3.5.0.jar,/opt/spark-extra-jars/spark-token-provider-kafka-0-10_2.12-3.5.0.jar,/opt/spark-extra-jars/kafka-clients-3.5.0.jar,/opt/spark-extra-jars/commons-pool2-2.12.0.jar,/opt/spark-extra-jars/hadoop-aws-3.3.4.jar,/opt/spark-extra-jars/aws-java-sdk-bundle-1.12.262.jar,/opt/spark-extra-jars/iceberg-spark-runtime-3.5_2.12-1.5.0.jar
      /home/jovyan/work/apps/bronze_cdc_table.py
    environment:
      BRONZE_TABLE: customers
      KAFKA_BOOTSTRAP: ${KAFKA_BOOTSTRAP:-northwind-kafka:9092}
      MINIO_ENDPOINT: ${MINIO_ENDPOINT:-http://northwind-minio:9000}
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      BRONZE_BUCKET: ${BRONZE_BUCKET:-iceberg-warehouse}
    volumes:
      - ./apps:/home/jovyan/work/apps
    networks:
      - spark-network
      - data-platform-network
    depends_on:
      - spark-master
      - spark-worker
    mem_limit: 1g
    restart: unless-stopped

  bronze-products:
    image: northwind-jupyter:3.5.0
    container_name: northwind-bronze-products
    command: >
      /usr/local/spark/bin/spark-submit
      --master spark://spark-master:7077
      --jars
      /opt/spark-extra-jars/spark-sql-kafka-0-10_2.12-3.5.0.jar,/opt/spark-extra-jars/spark-token-provider-kafka-0-10_2.12-3.5.0.jar,/opt/spark-extra-jars/kafka-clients-3.5.0.jar,/opt/spark-extra-jars/commons-pool2-2.12.0.jar,/opt/spark-extra-jars/hadoop-aws-3.3.4.jar,/opt/spark-extra-jars/aws-java-sdk-bundle-1.12.262.jar,/opt/spark-extra-jars/iceberg-spark-runtime-3.5_2.12-1.5.0.jar
      /home/jovyan/work/apps/bronze_cdc_table.py
    environment:
      BRONZE_TABLE: products
      KAFKA_BOOTSTRAP: ${KAFKA_BOOTSTRAP:-northwind-kafka:9092}
      MINIO_ENDPOINT: ${MINIO_ENDPOINT:-http://northwind-minio:9000}
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      BRONZE_BUCKET: ${BRONZE_BUCKET:-iceberg-warehouse}
    volumes:
      - ./apps:/home/jovyan/work/apps
    networks:
      - spark-network
      - data-platform-network
    depends_on:
      - spark-master
      - spark-worker
    mem_limit: 1g
    restart: unless-stopped

  bronze-orders:
    image: northwind-jupyter:3.5.0
    container_name: northwind-bronze-orders
    command: >
      /usr/local/spark/bin/spark-submit
      --master spark://spark-master:7077
      --jars
      /opt/spark-extra-jars/spark-sql-kafka-0-10_2.12-3.5.0.jar,/opt/spark-extra-jars/spark-token-provider-kafka-0-10_2.12-3.5.0.jar,/opt/spark-extra-jars/kafka-clients-3.5.0.jar,/opt/spark-extra-jars/commons-pool2-2.12.0.jar,/opt/spark-extra-jars/hadoop-aws-3.3.4.jar,/opt/spark-extra-jars/aws-java-sdk-bundle-1.12.262.jar,/opt/spark-extra-jars/iceberg-spark-runtime-3.5_2.12-1.5.0.jar
      /home/jovyan/work/apps/bronze_cdc_table.py
    environment:
      BRONZE_TABLE: orders
      KAFKA_BOOTSTRAP: ${KAFKA_BOOTSTRAP:-northwind-kafka:9092}
      MINIO_ENDPOINT: ${MINIO_ENDPOINT:-http://northwind-minio:9000}
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      BRONZE_BUCKET: ${BRONZE_BUCKET:-iceberg-warehouse}
    volumes:
      - ./apps:/home/jovyan/work/apps
    networks:
      - spark-network
      - data-platform-network
    depends_on:
      - spark-master
      - spark-worker
    mem_limit: 1g
    restart: unless-stopped

  bronze-order-details:
    image: northwind-jupyter:3.5.0
    container_name: northwind-bronze-order-details
    command: >
      /usr/local/spark/bin/spark-submit
      --master spark://spark-master:7077
      --jars
      /opt/spark-extra-jars/spark-sql-kafka-0-10_2.12-3.5.0.jar,/opt/spark-extra-jars/spark-token-provider-kafka-0-10_2.12-3.5.0.jar,/opt/spark-extra-jars/kafka-clients-3.5.0.jar,/opt/spark-extra-jars/commons-pool2-2.12.0.jar,/opt/spark-extra-jars/hadoop-aws-3.3.4.jar,/opt/spark-extra-jars/aws-java-sdk-bundle-1.12.262.jar,/opt/spark-extra-jars/iceberg-spark-runtime-3.5_2.12-1.5.0.jar
      /home/jovyan/work/apps/bronze_cdc_table.py
    environment:
      BRONZE_TABLE: order_details
      KAFKA_BOOTSTRAP: ${KAFKA_BOOTSTRAP:-northwind-kafka:9092}
      MINIO_ENDPOINT: ${MINIO_ENDPOINT:-http://northwind-minio:9000}
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      BRONZE_BUCKET: ${BRONZE_BUCKET:-iceberg-warehouse}
    volumes:
      - ./apps:/home/jovyan/work/apps
    networks:
      - spark-network
      - data-platform-network
    depends_on:
      - spark-master
      - spark-worker
    mem_limit: 1g
    restart: unless-stopped

networks:
  spark-network:
    external: true
    name: spark-network
  data-platform-network:
    external: true
    name: data-platform-network

volumes:
  ivy-cache-spark:
    driver: local
  ivy-cache-jupyter:
    driver: local