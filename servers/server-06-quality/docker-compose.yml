name: northwind-quality

services:
  prometheus:
    image: prom/prometheus:v2.53.1
    container_name: northwind-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus:/etc/prometheus
      - prometheus_data:/prometheus
    networks:
      - quality-network
      - data-platform-network
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
    restart: unless-stopped
    user: root

  grafana:
    image: grafana/grafana:10.4.5
    container_name: northwind-grafana
    # Set GRAFANA_ADMIN_USER / GRAFANA_ADMIN_PASSWORD in root .env (start-all.sh uses --env-file).
    # GF_SECURITY_ADMIN_* apply only on first init of grafana_data; reset volume or use UI to change.
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD}
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/dashboards:/etc/grafana/provisioning/dashboards
      - ./grafana/datasources:/etc/grafana/provisioning/datasources
    depends_on:
      - prometheus
    networks:
      - quality-network
      - data-platform-network
    restart: unless-stopped
  
  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:v0.15.0
    container_name: northwind-postgres-exporter
    environment:
      DATA_SOURCE_NAME: "${POSTGRES_EXPORTER_DSN}"
    ports:
      - "9187:9187"
    networks:
      - quality-network
      - data-platform-network
    restart: unless-stopped
  
volumes:
  prometheus_data:
  grafana_data:

networks:
  quality-network:
    external: true
  data-platform-network:
    external: true
